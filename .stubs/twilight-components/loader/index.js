// Stub loader for @salla.sa/twilight-components/loader
// Provides no-op functions so the theme's twilight.js can import without errors.
export function applyPolyfills() {
  return Promise.resolve();
}

export function defineCustomElements() {
  return Promise.resolve();
}
